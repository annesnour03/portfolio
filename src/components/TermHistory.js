import { useState } from 'react'
function TermHistory(input) {
    const [history, setHistory] = useState([])
    // setHistory(input => [...history,input])
	
	console.log(history);




}

export default TermHistory